import 'reflect-metadata';
import { Application } from '@gabliam/core';
import * as path from 'path';<% pluginsBootstrap.forEach(function(plugin) { %>
<%- 'import ' + plugin.name + ' from \'' + plugin.importPath + '\';'; -%>
<% }); %>


@Application({
  scanPath: __dirname,
  config: path.resolve(__dirname, '../config'),<% if (pluginsBootstrap.length) { %>
  plugins: [<% pluginsBootstrap.forEach(function(plugin, i) { %><% if (i !== 0) { %><%- ', ' -%><% } %><%- plugin.name -%><% }); %>]<% } %>
})
export class App {}
